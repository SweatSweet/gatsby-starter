import React from 'react'
import * as styles from './Preloader.module.scss'

const Preloader = () => {
  return <div data-animation='preloader' className={styles.preload}></div>
}

export default Preloader
